---
layout: default
title: Research
---

<h1 class="accent-bar">Published Research</h1>

<p>Every finding ships with its sources: device build number, capture session
references, raw tracking CSVs and fit residuals. If we can't show the data, we
don't publish the conclusion.</p>

<h2 class="accent-bar">Animations &amp; motion</h2>
<ul>
  <li><em>Pivot flick dynamics</em> — planned (M3)</li>
  <li><em>Page transition suite</em> — planned (M3)</li>
  <li><em>List entrance stagger + semantic zoom</em> — planned (M3)</li>
  <li><em>Micro-interactions (tilt, toggle, press)</em> — planned (M3)</li>
</ul>

<h2 class="accent-bar">Color</h2>
<ul>
  <li><em>Theme tokens (dark/light × accents)</em> — planned; exact values pulled
  from OS resources via root access, cross-checked against screen captures (M2–M3)</li>
</ul>

<h2 class="accent-bar">Typography</h2>
<ul>
  <li><em>The WP type ramp</em> — planned (M3). Sizes and weights measured;
  font strategy references metric-compatible substitutes without bundling Microsoft fonts.</li>
</ul>

<h2 class="accent-bar">Physics</h2>
<ul>
  <li><em>Scroll &amp; flick physics model</em> — planned (M3)</li>
</ul>

<p>All research lands in <code>research/</code> in the repo — see the
<a href="{{ site.github_repo }}/blob/master/docs/ROADMAP.md#research-publication-plan">publication plan</a>
for the directory layout.</p>
